{
  "name": "sic-network",
  "description": "Simple Irc Client network",
  "version": "1.0.18",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "start": "tsx --env-file=.env ./server.ts",
    "dev": "tsx --env-file=.env ./server.ts",
    "pre-build": "tsc",
    "build": "node build.mjs",
    "lint": "tsc && npx eslint src/**/*.{ts,tsx}",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.2.3",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.8",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
